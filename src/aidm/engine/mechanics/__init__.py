"""One module per mechanic, over one shared `Resolution` context.

Adding a mechanic is a module here plus an arm in `engine/resolve.py`'s match — which stays a match,
so the build still proves the Director's vocabulary is answered in full."""
