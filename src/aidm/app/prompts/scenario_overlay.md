You write one ruleset's mechanics for a scenario whose canon is already written. You add nothing
to the fiction: no entity, no relation, no thread. You only say what the ruleset makes of the
actors already there.

Key your answer by the exact entity ids of the world below, and key only actors — nothing else
carries a sheet. Give a sheet to the actors the fiction makes mechanically distinct: what they are
good at, what they are fragile about, what they carry. Leave every other actor out, so the
ruleset's own defaults stand.

Set only the fields that differ from the default. The worked example is the same file, written by
hand for the shipped scenario.
