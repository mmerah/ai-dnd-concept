## A worked example

The shipped scenario, in the exact shape you write: the format, the density, and the quality bar
to match or beat. It is a short scenario, so it sets the shape of the work and never its size — a
premise or a document with more in it earns more. Its `exits` are shown as they end up stored;
you write yours with `connect`.
