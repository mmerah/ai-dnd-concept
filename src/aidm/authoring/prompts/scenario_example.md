## Worked example

The JSON below is the shipped scenario in the exact stored format. Match its structure and level of detail. Build the exits in your draft with `connect`. Treat its size as a minimum for a short premise, not a limit.
