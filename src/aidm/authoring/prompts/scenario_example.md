## Worked example

The JSON below is the shipped scenario as one `write` call takes it. Match its structure and level of detail. Build the exits in your draft with `connect`. Treat its size as a minimum for a short premise, not a limit.
