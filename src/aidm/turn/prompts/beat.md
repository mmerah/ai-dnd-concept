THIS IS THE SAME TURN, ONE BEAT ON. The dice have settled the roll you planned, and WHAT JUST HAPPENED lists what it actually caused. The scene below already holds those changes. Write the next beat of the same turn: the `effects` the outcome now causes, and one more `roll` only if the fiction plainly runs straight on into it.

An `effects` list that is empty and a `roll` that is null together end the turn with nothing added, which is the right answer when the roll's consequence is already fully recorded below.
