# 24XX

A rules-light SF/adventure system: one skill die from d6 to d12, +d6 for help, d4 when hindered,
keep the highest, three outcome bands.

## Official sources

- SRD (v1.4, the text this repo's engine was built from):
  <https://24xx-srd.carrd.co/>
- Official downloads — plain text, PDF, layout templates, vector logo (v1.41 at time of writing):
  <https://jasontocci.itch.io/24xx>
- Explanatory mirror of a slightly different revision, with `rules/`, `characters/`, `gear/`,
  `details/` and `gm-page/` pages: <https://keeper.farirpgs.com/resources/jason-tocci/24xx/>
- The 200+ hacks at <https://itch.io/c/1204990/24xx> are separately licensed and are not drop-in
  packs.

This file used to hold a near-verbatim extraction of the SRD. It was deleted so that no pack is
ever transcribed out of a copy: build from the official page above. The old text is in git history.

## Licence and attribution

Creative Commons Attribution 4.0 (<https://creativecommons.org/licenses/by/4.0/>), text by
Jason Tocci. The required credit is short and already ships verbatim at
`src/aidm/engines/twentyfourxx/rules.md:3`:

> 24XX rules (v1.4) are CC BY Jason Tocci.

The SRD states three conditions on use: give credit, including the version number, somewhere in
the game; call it "24XX" rather than "2400" and claim no part of the 2400 series; and no bigotry.

## Pack sources

No separate pack sources. Specialties, origins and the 17 skills are all in the SRD at
<https://24xx-srd.carrd.co/>, transcribed into `packs/srd.json`.

## The tools

- `change_world` — reveal someone found, bring a cast member in or out of the scene, record a
  death, change the player's hindrances, gain or drop an item, repair broken gear, or spend
  credits.
- `next_scene` — say the scene's question is settled, or that the player left it; the player is
  then asked what they pursue.
- `attempt` — roll a skill (or the plain d6 when none applies) on the 1–2 / 3–4 / 5+ ladder, with
  help or hindrance when they apply.
- `test_luck` — one d6, read as trouble now / signs of it / nothing, for a question about the
  world where nobody acts.
- `defend` — break a carried item to turn a hit into a hindrance instead of taking it outright.
- `job_done` — once per job, when the player's own words close the job: raise the named
  skill and pay out its credits.

## Deviations in this repo

Every divergence between `src/aidm/engines/twentyfourxx/` and the official rules, with the reason
it stands. Nothing diverges silently: a rule not listed here is implemented as printed.

1. **No ally rolls.** An NPC carries no dice; an ally who pitches in helps with the d6 of
   circumstance the SRD gives any helper, not with a die of their own — the SRD prints no sheet
   for anyone but the operator.
2. **No succession on death.** The SRD's "If killed, make a new character to introduce ASAP" is
   not modelled: mid-game character creation has no procedure here, so dying ends the game, as it
   does in the other three engines.
3. **The d6 job-finding setup is prompt guidance for a campaign's board, not a roll; the d20
   detail tables stay unmodelled.** Both are content the SRD itself offers as "invent or roll",
   and the board is authored by the worldsmith rather than rolled at the table.
4. **The android body is a trait, not an item.** Origin: Android's "case" cannot take a `defend`
   break; its printed defense use is not modelled, since traits carry no mechanical hooks here.
5. **The priced gear table and the ship rules are not transcribed.** Buying is play through
   `gain_item` and `spend`, with the SRD's list as the master's vocabulary; the ship, its
   systems and their upgrades have no layer here at all.

Three readings the SRD leaves open are settled without diverging from it: help and hindrance may
both apply to the same roll, since the SRD covers each in its own sentence and never says they are
exclusive; a job is one outing from the hub in a campaign, the whole adventure in a one-shot, so
`job_done` fires once per job, from the player's own words that the work is finished; and a luck
test is one d6 read on trouble now / signs of it / nothing, the SRD's "interpret the result as you
see fit" made concrete.

## What the AI game master adds

Fields and tools that exist for the app around the rules, not the rules themselves.

- `known`, `hidden` and the scene `secret` — the told-fact gate: no unknown name reaches the
  narrator.
- `alive` and `kill` — the SRD leaves being taken out or dying to the table; the gate needs a
  flag, and `risking_death` hands the master that ruling as a ladder result rather than a note.
- `next_scene`, `settled` and the turn cap — a scene nobody ends is ended for them.
- `hindrances` as a plain list on the sheet — the SRD's word for what slows the player down, kept
  as data so the narrator can read it back.

## Where the rules live

Mechanics are in `src/aidm/engines/twentyfourxx/`; the scene machinery shared with the other
scene engines is `SceneEngine` in `src/aidm/engines/scenes/`. `packs/srd.json` — not this file —
is the transcription of record.
