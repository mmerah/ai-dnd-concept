[
  {
    "name": "change_world",
    "description": "Apply one settled world change to match the story. Set `verb` to pick the change and fill that verb's own fields. One call makes one change.",
    "parameters": {
      "$defs": {
        "JoinParty": {
          "additionalProperties": false,
          "description": "A character here starts travelling with the player.",
          "properties": {
            "verb": {
              "const": "join_party",
              "type": "string"
            },
            "entity_id": {
              "description": "Exact id of who is joining.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "entity_id"
          ],
          "title": "JoinParty",
          "type": "object"
        },
        "Kill": {
          "additionalProperties": false,
          "properties": {
            "verb": {
              "const": "kill",
              "type": "string"
            },
            "entity_id": {
              "description": "Exact id of an npc here.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "entity_id"
          ],
          "title": "Kill",
          "type": "object"
        },
        "LeaveParty": {
          "additionalProperties": false,
          "description": "A companion stops travelling with the player.",
          "properties": {
            "verb": {
              "const": "leave_party",
              "type": "string"
            },
            "entity_id": {
              "description": "Exact id of the companion leaving.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "entity_id"
          ],
          "title": "LeaveParty",
          "type": "object"
        },
        "MoveItem": {
          "additionalProperties": false,
          "properties": {
            "verb": {
              "const": "move_item",
              "type": "string"
            },
            "item_id": {
              "description": "Exact id of an item here or carried.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            "to": {
              "description": "Exact id of the player, an npc here, or this place.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "item_id",
            "to"
          ],
          "title": "MoveItem",
          "type": "object"
        },
        "Reveal": {
          "additionalProperties": false,
          "properties": {
            "verb": {
              "const": "reveal",
              "type": "string"
            },
            "entity_id": {
              "description": "Exact id of something hidden here: an npc or an item.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "entity_id"
          ],
          "title": "Reveal",
          "type": "object"
        }
      },
      "additionalProperties": false,
      "properties": {
        "change": {
          "description": "The one world change to apply; `verb` picks the change.",
          "discriminator": {
            "mapping": {
              "join_party": "#/$defs/JoinParty",
              "kill": "#/$defs/Kill",
              "leave_party": "#/$defs/LeaveParty",
              "move_item": "#/$defs/MoveItem",
              "reveal": "#/$defs/Reveal"
            },
            "propertyName": "verb"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/Reveal"
            },
            {
              "$ref": "#/$defs/MoveItem"
            },
            {
              "$ref": "#/$defs/Kill"
            },
            {
              "$ref": "#/$defs/JoinParty"
            },
            {
              "$ref": "#/$defs/LeaveParty"
            }
          ]
        }
      },
      "required": [
        "change"
      ],
      "type": "object"
    }
  },
  {
    "name": "move",
    "description": "Move through an unlocked way from the player's current place.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "to_id": {
          "description": "Exact id of the place to move to.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "with_ids": {
          "default": [],
          "description": "Exact ids of living NPCs here who come along.",
          "items": {
            "maxLength": 64,
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "to_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "unlock_way",
    "description": "Unlock a locked way out of the player's current place.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "to_id": {
          "description": "Exact id of the locked way's destination.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "required": [
        "to_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "action_roll",
    "description": "Roll 2d6 plus an ability and helpful items against a Difficulty Score or an npc; `actor_id` when a hired member acts instead of the player.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "what": {
          "description": "The attempt, in a few words the player may read; it heads the card.",
          "minLength": 1,
          "type": "string"
        },
        "ability": {
          "description": "Which ability the action calls on.",
          "enum": [
            "brute",
            "skulker",
            "erudite"
          ],
          "type": "string"
        },
        "items": {
          "default": [],
          "description": "Exact ids of items the actor carries that plainly help; +1 each.",
          "items": {
            "maxLength": 64,
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
            "type": "string"
          },
          "type": "array"
        },
        "difficulty": {
          "anyOf": [
            {
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Difficulty Score; the SRD's guidelines: 8 easy, 10 moderate, 12 hard. Null when `against` names an NPC."
        },
        "against": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of an npc here; its Health is the Difficulty Score."
        },
        "dangerous": {
          "default": false,
          "description": "A fight, a trap, a fall: the margin becomes damage, to the NPC on a hit or to the actor on a miss.",
          "type": "boolean"
        },
        "actor_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "null for the player; else the exact id of a hired party member here who acts."
        }
      },
      "required": [
        "what",
        "ability"
      ],
      "type": "object"
    }
  },
  {
    "name": "rest",
    "description": "Spend the night in a safe spot to heal the player's and the party's Health to full.",
    "parameters": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "level_up",
    "description": "Raise one ability and either Health or Inventory Score by 1, once, at the adventure's end: the player first, then each hired member in turn.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "ability": {
          "anyOf": [
            {
              "enum": [
                "brute",
                "skulker",
                "erudite"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Which ability to raise by 1; null asks the player."
        },
        "boost": {
          "anyOf": [
            {
              "enum": [
                "health",
                "inventory"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Health or Inventory to raise by 1; null asks the player."
        },
        "actor_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "null for the player; else the exact id of a hired party member here who acts."
        }
      },
      "type": "object"
    }
  },
  {
    "name": "hire",
    "description": "The player hires someone here to work: the worldsmith writes their sheet once this turn ends, and they join the party. Someone already travelling with the player may be hired too; a sheet is for someone hired to work, never for one who merely comes along.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "entity_id": {
          "description": "Exact id of who here signs on; they must not already carry a sheet.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "terms": {
          "description": "What they are hired for and on what terms, as agreed, for the worldsmith.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "entity_id",
        "terms"
      ],
      "type": "object"
    }
  }
]
