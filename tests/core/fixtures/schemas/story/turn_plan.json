{
  "$defs": {
    "AddRelation": {
      "additionalProperties": false,
      "description": "Record a lasting tie between two entities that is not containment: carrying an item or\nstanding somewhere are moves, not relations.",
      "properties": {
        "op": {
          "const": "add-relation",
          "default": "add-relation",
          "title": "Op",
          "type": "string"
        },
        "kind": {
          "description": "What the tie is: `connected` joins two locations the player can walk between, `party-member` puts an actor here into the player's party (source is the actor, target is `player`).",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Kind",
          "type": "string"
        },
        "source": {
          "description": "Exact id of the tie's source entity.",
          "title": "Source",
          "type": "string"
        },
        "target": {
          "description": "Exact id of the tie's target entity.",
          "title": "Target",
          "type": "string"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "source",
        "target"
      ],
      "title": "AddRelation",
      "type": "object"
    },
    "AddTag": {
      "additionalProperties": false,
      "description": "Put a lasting condition, edge, or burden on an entity. The sheet shows the id written\nout: `battle-worn` appears as Battle Worn.",
      "properties": {
        "op": {
          "const": "add-tag",
          "default": "add-tag",
          "title": "Op",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the entity affected; an actor must be here with the player.",
          "title": "Entity Id",
          "type": "string"
        },
        "tag_id": {
          "description": "Stable slug for the tag, such as `poisoned`.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Tag Id",
          "type": "string"
        },
        "text": {
          "default": "",
          "description": "The constraint or benefit it puts on the entity, in prose.",
          "title": "Text",
          "type": "string"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "entity_id",
        "tag_id"
      ],
      "title": "AddTag",
      "type": "object"
    },
    "AdjustCounter": {
      "additionalProperties": false,
      "description": "Move a counter up or down. The change is clamped to the counter's own bounds.",
      "properties": {
        "op": {
          "const": "adjust-counter",
          "default": "adjust-counter",
          "title": "Op",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the entity affected; an actor must be here with the player.",
          "title": "Entity Id",
          "type": "string"
        },
        "counter": {
          "description": "Exact key of one of that entity's counters, as its sheet spells it.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Counter",
          "type": "string"
        },
        "delta": {
          "description": "How much the pool moves: negative to reduce.",
          "title": "Delta",
          "type": "integer"
        },
        "maximum": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A new upper bound for the pool. Advancement only.",
          "title": "Maximum"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "entity_id",
        "counter",
        "delta"
      ],
      "title": "AdjustCounter",
      "type": "object"
    },
    "AdvanceThread": {
      "additionalProperties": false,
      "description": "Move a storyline the scenario is tracking: where it stands now, or that it is over.",
      "properties": {
        "op": {
          "const": "advance-thread",
          "default": "advance-thread",
          "title": "Op",
          "type": "string"
        },
        "thread_id": {
          "description": "Exact id of one thread in ACTIVE THREADS.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Thread Id",
          "type": "string"
        },
        "status": {
          "anyOf": [
            {
              "enum": [
                "active",
                "resolved",
                "dormant"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Where the thread now stands, or null to leave it as it is.",
          "title": "Status"
        },
        "stage": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Stable slug for the point it has reached, or null to leave it as it is.",
          "title": "Stage"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "thread_id"
      ],
      "title": "AdvanceThread",
      "type": "object"
    },
    "GainImprovisedItem": {
      "additionalProperties": false,
      "description": "Give the player an ordinary incidental object that is not in canon and is not worth a canon\nentry of its own. Never a substitute for an item that already exists.",
      "properties": {
        "op": {
          "const": "gain-improvised-item",
          "default": "gain-improvised-item",
          "title": "Op",
          "type": "string"
        },
        "item_name": {
          "description": "The object written out, such as 'a handful of gravel'.",
          "minLength": 1,
          "title": "Item Name",
          "type": "string"
        }
      },
      "required": [
        "item_name"
      ],
      "title": "GainImprovisedItem",
      "type": "object"
    },
    "MoveActor": {
      "additionalProperties": false,
      "description": "Move an actor who actually changes location. Moving the player to an unrevealed location\nreveals it.",
      "properties": {
        "op": {
          "const": "move-actor",
          "default": "move-actor",
          "title": "Op",
          "type": "string"
        },
        "entity_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of the actor to move; null moves the player.",
          "title": "Entity Id"
        },
        "location_id": {
          "description": "Exact id of the location the actor enters.",
          "title": "Location Id",
          "type": "string"
        }
      },
      "required": [
        "location_id"
      ],
      "title": "MoveActor",
      "type": "object"
    },
    "MoveItem": {
      "additionalProperties": false,
      "description": "Move one item within the player's reach: pick it up, set it down here, or hand it to an\nactor here.",
      "properties": {
        "op": {
          "const": "move-item",
          "default": "move-item",
          "title": "Op",
          "type": "string"
        },
        "item_id": {
          "description": "Exact id of the item: one the player carries, or one loose at their location.",
          "title": "Item Id",
          "type": "string"
        },
        "to_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of the receiver: an actor here with the player, or the player's own location to set the item down. Null hands the item to the player.",
          "title": "To Id"
        }
      },
      "required": [
        "item_id"
      ],
      "title": "MoveItem",
      "type": "object"
    },
    "OutcomeBranch": {
      "additionalProperties": false,
      "description": "What follows in the fiction if the action lands on this outcome, and only then.",
      "properties": {
        "outcome": {
          "description": "One outcome label the chosen action allows.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Outcome",
          "type": "string"
        },
        "effects": {
          "default": [],
          "description": "What that outcome causes in the world.",
          "items": {
            "$ref": "#/$defs/TurnEffect"
          },
          "title": "Effects",
          "type": "array"
        }
      },
      "required": [
        "outcome"
      ],
      "title": "OutcomeBranch",
      "type": "object"
    },
    "RemoveRelation": {
      "additionalProperties": false,
      "description": "Break a lasting tie that `add-relation` recorded.",
      "properties": {
        "op": {
          "const": "remove-relation",
          "default": "remove-relation",
          "title": "Op",
          "type": "string"
        },
        "kind": {
          "description": "Exact kind of the tie, such as `connected`.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Kind",
          "type": "string"
        },
        "source": {
          "description": "Exact id of the tie's source entity.",
          "title": "Source",
          "type": "string"
        },
        "target": {
          "description": "Exact id of the tie's target entity.",
          "title": "Target",
          "type": "string"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "source",
        "target"
      ],
      "title": "RemoveRelation",
      "type": "object"
    },
    "RemoveTag": {
      "additionalProperties": false,
      "description": "Lift a tag an entity carries, when the fiction ends it.",
      "properties": {
        "op": {
          "const": "remove-tag",
          "default": "remove-tag",
          "title": "Op",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the entity affected; an actor must be here with the player.",
          "title": "Entity Id",
          "type": "string"
        },
        "tag_id": {
          "description": "Exact id of a tag the entity carries.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Tag Id",
          "type": "string"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "entity_id",
        "tag_id"
      ],
      "title": "RemoveTag",
      "type": "object"
    },
    "Reveal": {
      "additionalProperties": false,
      "description": "Reveal an entity that exists but the player does not know yet: they notice it, are told of\nit, or reach it. Prefer this over inventing a replacement.",
      "properties": {
        "op": {
          "const": "reveal",
          "default": "reveal",
          "title": "Op",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the unrevealed canon entity.",
          "title": "Entity Id",
          "type": "string"
        }
      },
      "required": [
        "entity_id"
      ],
      "title": "Reveal",
      "type": "object"
    },
    "RevealRelation": {
      "additionalProperties": false,
      "description": "Show the player a way through they did not know about: the connection equivalent of\n`reveal`. Write this before moving the player through a passage they have not found yet.",
      "properties": {
        "op": {
          "const": "reveal-relation",
          "default": "reveal-relation",
          "title": "Op",
          "type": "string"
        },
        "kind": {
          "description": "Exact kind of the tie, such as `connected`.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Kind",
          "type": "string"
        },
        "source": {
          "description": "Exact id of the tie's source entity.",
          "title": "Source",
          "type": "string"
        },
        "target": {
          "description": "Exact id of the tie's target entity.",
          "title": "Target",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "source",
        "target"
      ],
      "title": "RevealRelation",
      "type": "object"
    },
    "Risk": {
      "additionalProperties": false,
      "description": "The one thing this engine resolves: an uncertain attempt, settled by 2d6 against a 7.",
      "properties": {
        "act": {
          "const": "risk",
          "default": "risk",
          "title": "Act",
          "type": "string"
        },
        "actor_id": {
          "description": "Exact id of the actor taking the risk: the player, or an actor here.",
          "title": "Actor Id",
          "type": "string"
        },
        "approach": {
          "description": "How they go about it; that approach's number on their sheet is the bonus.",
          "enum": [
            "bold",
            "subtle",
            "clever",
            "empathetic"
          ],
          "title": "Approach",
          "type": "string"
        },
        "difficulty": {
          "description": "How hard it is: risky costs nothing, demanding -1, extreme -2.",
          "enum": [
            "risky",
            "demanding",
            "extreme"
          ],
          "title": "Difficulty",
          "type": "string"
        },
        "helping_tag_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of the one tag that directly helps — an edge, a bond, or a gear benefit on an item that actor carries. Null when none does.",
          "title": "Helping Tag Id"
        },
        "hindering_tag_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of the one tag on that actor's sheet that directly hinders — a burden or a condition. Null when none does.",
          "title": "Hindering Tag Id"
        },
        "stakes": {
          "description": "What is attempted, in a few words; it names the roll.",
          "minLength": 1,
          "title": "Stakes",
          "type": "string"
        }
      },
      "required": [
        "actor_id",
        "approach",
        "difficulty",
        "stakes"
      ],
      "title": "Risk",
      "type": "object"
    },
    "SpendCounter": {
      "additionalProperties": false,
      "description": "Pay from a counter, which refuses outright when the pool cannot cover it.",
      "properties": {
        "op": {
          "const": "spend-counter",
          "default": "spend-counter",
          "title": "Op",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the entity affected; an actor must be here with the player.",
          "title": "Entity Id",
          "type": "string"
        },
        "counter": {
          "description": "Exact key of one of that entity's counters, as its sheet spells it.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Counter",
          "type": "string"
        },
        "amount": {
          "description": "How much of the pool is spent.",
          "minimum": 1,
          "title": "Amount",
          "type": "integer"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "entity_id",
        "counter",
        "amount"
      ],
      "title": "SpendCounter",
      "type": "object"
    },
    "TurnEffect": {
      "discriminator": {
        "mapping": {
          "add-relation": "#/$defs/AddRelation",
          "add-tag": "#/$defs/AddTag",
          "adjust-counter": "#/$defs/AdjustCounter",
          "advance-thread": "#/$defs/AdvanceThread",
          "gain-improvised-item": "#/$defs/GainImprovisedItem",
          "move-actor": "#/$defs/MoveActor",
          "move-item": "#/$defs/MoveItem",
          "remove-relation": "#/$defs/RemoveRelation",
          "remove-tag": "#/$defs/RemoveTag",
          "reveal": "#/$defs/Reveal",
          "reveal-relation": "#/$defs/RevealRelation",
          "spend-counter": "#/$defs/SpendCounter",
          "untag-relation": "#/$defs/UntagRelation"
        },
        "propertyName": "op"
      },
      "oneOf": [
        {
          "$ref": "#/$defs/Reveal"
        },
        {
          "$ref": "#/$defs/MoveActor"
        },
        {
          "$ref": "#/$defs/MoveItem"
        },
        {
          "$ref": "#/$defs/GainImprovisedItem"
        },
        {
          "$ref": "#/$defs/AdjustCounter"
        },
        {
          "$ref": "#/$defs/SpendCounter"
        },
        {
          "$ref": "#/$defs/AddTag"
        },
        {
          "$ref": "#/$defs/RemoveTag"
        },
        {
          "$ref": "#/$defs/AddRelation"
        },
        {
          "$ref": "#/$defs/RemoveRelation"
        },
        {
          "$ref": "#/$defs/UntagRelation"
        },
        {
          "$ref": "#/$defs/RevealRelation"
        },
        {
          "$ref": "#/$defs/AdvanceThread"
        }
      ]
    },
    "UntagRelation": {
      "additionalProperties": false,
      "description": "Lift a tag a tie carries, when the fiction ends it.",
      "properties": {
        "op": {
          "const": "untag-relation",
          "default": "untag-relation",
          "title": "Op",
          "type": "string"
        },
        "kind": {
          "description": "Exact kind of the tie, such as `connected`.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Kind",
          "type": "string"
        },
        "source": {
          "description": "Exact id of the tie's source entity.",
          "title": "Source",
          "type": "string"
        },
        "target": {
          "description": "Exact id of the tie's target entity.",
          "title": "Target",
          "type": "string"
        },
        "tag": {
          "description": "Exact id of a tag the tie carries.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Tag",
          "type": "string"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "source",
        "target",
        "tag"
      ],
      "title": "UntagRelation",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "effects": {
      "default": [],
      "description": "Consequences that happen whatever the action settles.",
      "items": {
        "$ref": "#/$defs/TurnEffect"
      },
      "title": "Effects",
      "type": "array"
    },
    "branches": {
      "default": [],
      "description": "Consequences keyed by outcome: at most one branch per label, and only labels this action allows. Outcomes that do not occur simply never apply. An action rolled to cause a lasting change — a condition taking hold or lifted, something revealed — must carry that change in the matching outcome's branch: with no branch, even success changes nothing.",
      "items": {
        "$ref": "#/$defs/OutcomeBranch"
      },
      "title": "Branches",
      "type": "array"
    },
    "action": {
      "anyOf": [
        {
          "$ref": "#/$defs/Risk"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The one risk this turn resolves, or null when nothing is uncertain enough to roll for."
    }
  },
  "title": "TurnPlan",
  "type": "object"
}
