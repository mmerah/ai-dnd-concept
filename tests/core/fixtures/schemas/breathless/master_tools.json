[
  {
    "name": "change_world",
    "description": "Apply one settled world change to match the story. Set `verb` to pick the change and fill that verb's own fields. One call makes one change.",
    "parameters": {
      "$defs": {
        "DropItem": {
          "additionalProperties": false,
          "description": "Take an item out of the actor's backpack for good.",
          "properties": {
            "verb": {
              "const": "drop_item",
              "type": "string"
            },
            "item_id": {
              "description": "Exact id of an item the actor carries.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            "actor_id": {
              "anyOf": [
                {
                  "maxLength": 64,
                  "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "null for the player; else the exact id of a hired survivor here who acts."
            }
          },
          "required": [
            "verb",
            "item_id"
          ],
          "title": "DropItem",
          "type": "object"
        },
        "Enter": {
          "additionalProperties": false,
          "description": "Bring a cast member into the current scene.",
          "properties": {
            "verb": {
              "const": "enter",
              "type": "string"
            },
            "entity_id": {
              "description": "Exact id of a cast member not already here.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "entity_id"
          ],
          "title": "Enter",
          "type": "object"
        },
        "JoinParty": {
          "additionalProperties": false,
          "description": "A character here starts travelling with the player.",
          "properties": {
            "verb": {
              "const": "join_party",
              "type": "string"
            },
            "entity_id": {
              "description": "Exact id of who is joining.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "entity_id"
          ],
          "title": "JoinParty",
          "type": "object"
        },
        "Kill": {
          "additionalProperties": false,
          "description": "Record that someone here has died.",
          "properties": {
            "verb": {
              "const": "kill",
              "type": "string"
            },
            "entity_id": {
              "description": "Exact id of who here died.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "entity_id"
          ],
          "title": "Kill",
          "type": "object"
        },
        "Leave": {
          "additionalProperties": false,
          "description": "Take a cast member out of the current scene.",
          "properties": {
            "verb": {
              "const": "leave",
              "type": "string"
            },
            "entity_id": {
              "description": "Exact id of someone here.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "entity_id"
          ],
          "title": "Leave",
          "type": "object"
        },
        "LeaveParty": {
          "additionalProperties": false,
          "description": "A companion stops travelling with the player.",
          "properties": {
            "verb": {
              "const": "leave_party",
              "type": "string"
            },
            "entity_id": {
              "description": "Exact id of the companion leaving.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "entity_id"
          ],
          "title": "LeaveParty",
          "type": "object"
        },
        "Reveal": {
          "additionalProperties": false,
          "description": "Make a hidden entity known when the player notices, finds, or reaches it.",
          "properties": {
            "verb": {
              "const": "reveal",
              "type": "string"
            },
            "entity_id": {
              "description": "Exact id of an entity listed as hidden here.",
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "verb",
            "entity_id"
          ],
          "title": "Reveal",
          "type": "object"
        }
      },
      "additionalProperties": false,
      "properties": {
        "change": {
          "description": "The one world change to apply; `verb` picks the change.",
          "discriminator": {
            "mapping": {
              "drop_item": "#/$defs/DropItem",
              "enter": "#/$defs/Enter",
              "join_party": "#/$defs/JoinParty",
              "kill": "#/$defs/Kill",
              "leave": "#/$defs/Leave",
              "leave_party": "#/$defs/LeaveParty",
              "reveal": "#/$defs/Reveal"
            },
            "propertyName": "verb"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/Reveal"
            },
            {
              "$ref": "#/$defs/Enter"
            },
            {
              "$ref": "#/$defs/Leave"
            },
            {
              "$ref": "#/$defs/Kill"
            },
            {
              "$ref": "#/$defs/JoinParty"
            },
            {
              "$ref": "#/$defs/LeaveParty"
            },
            {
              "$ref": "#/$defs/DropItem"
            }
          ]
        }
      },
      "required": [
        "change"
      ],
      "type": "object"
    }
  },
  {
    "name": "next_scene",
    "description": "Offer the player the way on: this scene has reached a useful stopping point. They are then asked what they want to pursue, and their own words build the next scene; they may also stay. Do not answer for them. Set `pursuit` instead once the player has left this place. Or set `complication` to bring a new situation down on this place, only when `change_world` (an arrival, a reveal, a death) cannot make it from what is here.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "pursuit": {
          "default": "",
          "description": "Set when the player has left this place for good: where they are going, in their own words. Empty to offer the way on from here.",
          "type": "string"
        },
        "complication": {
          "default": "",
          "description": "Set to change the situation here without the player leaving: what arrives or turns, and why, for the worldsmith. Written now, the turn ends; the player answers it next turn. Empty otherwise.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "check",
    "description": "Roll a check for an action with a real cost, on a skill, a carried item, or a stunt. `actor_id` when a hired survivor acts instead of the player; `helped_by` names a hired survivor who also makes a skill check on their own die and shares the risk.",
    "parameters": {
      "$defs": {
        "Skill": {
          "enum": [
            "bash",
            "dash",
            "sneak",
            "shoot",
            "think",
            "sway"
          ],
          "type": "string"
        }
      },
      "additionalProperties": false,
      "properties": {
        "what": {
          "description": "The attempt, in a few words the player may read; it heads the card.",
          "minLength": 1,
          "type": "string"
        },
        "actor_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "null for the player; else the exact id of a hired survivor here who acts."
        },
        "skill": {
          "anyOf": [
            {
              "$ref": "#/$defs/Skill"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Which of the six skills the action calls on."
        },
        "item_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of a carried item used instead of a skill."
        },
        "stunt": {
          "default": false,
          "description": "True to attempt an extraordinary stunt at d12 instead of a skill or item.",
          "type": "boolean"
        },
        "dangerous": {
          "default": false,
          "description": "True when a fail would plainly hurt the actor.",
          "type": "boolean"
        },
        "helped_by": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A hired survivor here who helps: they also make the check on their own die of the same skill and share its risks; the highest die counts. Only on a skill check."
        }
      },
      "required": [
        "what"
      ],
      "type": "object"
    }
  },
  {
    "name": "catch_breath",
    "description": "Let the actor catch their breath: skills, loot die and the stunt reset, at the cost of a new complication. `actor_id` when a hired survivor catches breath instead of the player.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "actor_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "null for the player; else the exact id of a hired survivor here who acts."
        }
      },
      "type": "object"
    }
  },
  {
    "name": "change_stress",
    "description": "A complication costs the actor stress; laying low somewhere secure clears an amount at your discretion. Never a stand-in for `use_med_kit`. `actor_id` when a hired survivor is meant instead of the player.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "amount": {
          "description": "How much stress changes: positive for a complication's cost, negative to clear it.",
          "type": "integer"
        },
        "why": {
          "description": "What causes the change, in a few words.",
          "minLength": 1,
          "type": "string"
        },
        "actor_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "null for the player; else the exact id of a hired survivor here who acts."
        }
      },
      "required": [
        "amount",
        "why"
      ],
      "type": "object"
    }
  },
  {
    "name": "use_med_kit",
    "description": "Spend the actor's med kit to clear 2 stress. `actor_id` when a hired survivor is meant instead of the player.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "actor_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "null for the player; else the exact id of a hired survivor here who acts."
        }
      },
      "type": "object"
    }
  },
  {
    "name": "loot_check",
    "description": "Scavenge for an item. Leave `granted` and `choice` null; the engine fills them once the player answers.",
    "parameters": {
      "$defs": {
        "Die": {
          "enum": [
            4,
            6,
            8,
            10,
            12
          ],
          "type": "integer"
        }
      },
      "additionalProperties": false,
      "properties": {
        "item": {
          "description": "What is found if the roll finds anything; the die sets how good it is.",
          "minLength": 1,
          "type": "string"
        },
        "granted": {
          "anyOf": [
            {
              "$ref": "#/$defs/Die"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Leave null; the engine fills it when the player answers."
        },
        "choice": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Leave null; the engine fills it when the player answers."
        }
      },
      "required": [
        "item"
      ],
      "type": "object"
    }
  },
  {
    "name": "test_luck",
    "description": "Roll a die to answer a question about the world where nobody is acting.",
    "parameters": {
      "$defs": {
        "Die": {
          "enum": [
            4,
            6,
            8,
            10,
            12
          ],
          "type": "integer"
        }
      },
      "additionalProperties": false,
      "properties": {
        "question": {
          "description": "A closed question about the world where nobody is acting.",
          "minLength": 1,
          "type": "string"
        },
        "die": {
          "$ref": "#/$defs/Die",
          "description": "Which die to roll, picked by the odds."
        }
      },
      "required": [
        "question",
        "die"
      ],
      "type": "object"
    }
  },
  {
    "name": "hire",
    "description": "The player hires someone here to work: the worldsmith writes their sheet once this turn ends, and they join the party. Someone already travelling with the player may be hired too; a sheet is for someone hired to work, never for one who merely comes along.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "entity_id": {
          "description": "Exact id of who here signs on; they must not already carry a sheet.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "terms": {
          "description": "What they are hired for and on what terms, as agreed, for the worldsmith.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "entity_id",
        "terms"
      ],
      "type": "object"
    }
  }
]
