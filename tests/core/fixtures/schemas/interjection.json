{
  "additionalProperties": false,
  "description": "What a party member says after a turn, unprompted.",
  "properties": {
    "lines": {
      "description": "What the party member says, as dialogue. Give no lines to stay quiet.",
      "items": {
        "additionalProperties": false,
        "properties": {
          "speaker_id": {
            "type": [
              "string",
              "null"
            ],
            "default": null,
            "description": "Exact id of the speaker. Null for narration."
          },
          "text": {
            "description": "One passage of narration, or only what the speaker says.",
            "type": "string"
          }
        },
        "required": [
          "text"
        ],
        "type": "object"
      },
      "type": "array"
    },
    "proposal": {
      "default": "",
      "description": "What the party member asks the party to do now. Leave empty to only speak.",
      "type": "string"
    }
  },
  "required": [
    "lines"
  ],
  "type": "object"
}
