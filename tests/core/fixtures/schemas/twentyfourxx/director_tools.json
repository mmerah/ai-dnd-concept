[
  {
    "name": "reveal",
    "description": "Make a hidden entity known when the player notices, finds, or reaches it.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "entity_id": {
          "description": "Exact id of the hidden entity.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "required": [
        "entity_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "move",
    "description": "Move an actor to a new location, or move a nearby item.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "entity_id": {
          "description": "Exact actor or item id. The item must be carried by the player or loose here.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "to_id": {
          "description": "Exact destination id. An actor moves to a location id. An item is taken by moving it to `player`, handed over by moving it to an actor here, and dropped by moving it to the id of the player's current location.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "required": [
        "entity_id",
        "to_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "gain_improvised_item",
    "description": "Give the player an ordinary, unimportant object not already in the world.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "item_name": {
          "description": "The object's name, such as `a handful of gravel`.",
          "type": "string"
        }
      },
      "required": [
        "item_name"
      ],
      "type": "object"
    }
  },
  {
    "name": "add_trait",
    "description": "Add a lasting condition or quality to an entity.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "entity_id": {
          "description": "Exact entity id. An actor must be here with the player.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "name": {
          "description": "The trait's display name, such as `Battle Worn`; its id is derived.",
          "minLength": 1,
          "type": "string"
        },
        "text": {
          "description": "The trait's effect in plain language.",
          "type": "string"
        }
      },
      "required": [
        "entity_id",
        "name",
        "text"
      ],
      "type": "object"
    }
  },
  {
    "name": "remove_trait",
    "description": "Remove a lasting condition or quality that has ended.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "entity_id": {
          "description": "Exact entity id. An actor must be here with the player.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "trait_id": {
          "description": "Exact id of one of the entity's traits.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "required": [
        "entity_id",
        "trait_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "kill",
    "description": "Record that an actor has died. Their body and what they carried stay in the world.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "actor_id": {
          "description": "Exact id of the actor who has died. They must be here with the player.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "required": [
        "actor_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "advance_thread",
    "description": "Update an active storyline's status, stage, clock, or note.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "thread_id": {
          "description": "Exact id of an ACTIVE THREAD.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "status": {
          "anyOf": [
            {
              "enum": [
                "active",
                "resolved",
                "dormant"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "New status, or null to keep the current status."
        },
        "stage": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "New stage slug, or null to keep the current stage."
        },
        "tick": {
          "default": 0,
          "description": "Number of segments to fill on its clock. Use 0 for no change.",
          "type": "integer"
        },
        "note": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "New private note for the Director, or null to keep the current note."
        }
      },
      "required": [
        "thread_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "unlock_exit",
    "description": "Unlock an exit from the player's location.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "to_id": {
          "description": "Exact id of the exit's destination.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "required": [
        "to_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "join_party",
    "description": "Add an actor here to the player's party.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "actor_id": {
          "description": "Exact id of the actor joining.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "required": [
        "actor_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "leave_party",
    "description": "Remove an actor from the player's party.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "actor_id": {
          "description": "Exact id of the actor leaving.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "required": [
        "actor_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "roll_attempt",
    "description": "Roll an actor's risky attempt directly. For the player, use `stake_attempt` first unless they already accepted the exact `risk`.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "actor_id": {
          "description": "Exact id of the player or actor here who takes the action.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "goal": {
          "description": "The actor's goal, in one line.",
          "minLength": 1,
          "type": "string"
        },
        "risk": {
          "description": "One-line cost of a bad roll. There is no roll without one.",
          "minLength": 1,
          "type": "string"
        },
        "hit": {
          "description": "True when a bad roll means physical harm to the actor.",
          "type": "boolean"
        },
        "skill": {
          "default": "",
          "description": "Matching skill from the actor's sheet, or empty for d6.",
          "type": "string"
        },
        "helped": {
          "default": "",
          "description": "Helpful circumstance that adds d6. Empty when using `helper_id` or no help.",
          "type": "string"
        },
        "helper_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of an ally here who adds their skill die, or null for no helper."
        },
        "helper_skill": {
          "default": "",
          "description": "Matching skill from the helper's sheet, or empty for their d6.",
          "type": "string"
        },
        "hindered": {
          "default": "",
          "description": "Hindering circumstance that lowers the actor's die to d4, such as an injury trait in the way or a heavy load; empty when nothing hinders.",
          "type": "string"
        },
        "luck_test": {
          "default": "",
          "description": "Separate bad luck that may arrive, or empty for no test.",
          "type": "string"
        }
      },
      "required": [
        "actor_id",
        "goal",
        "risk",
        "hit"
      ],
      "type": "object"
    }
  },
  {
    "name": "stake_attempt",
    "description": "Show the player one attempt's `risk` and let them accept or revise it before rolling.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "actor_id": {
          "description": "Exact id of the player or actor here who takes the action.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "goal": {
          "description": "The actor's goal, in one line.",
          "minLength": 1,
          "type": "string"
        },
        "risk": {
          "description": "One-line cost of a bad roll. There is no roll without one.",
          "minLength": 1,
          "type": "string"
        },
        "hit": {
          "description": "True when a bad roll means physical harm to the actor.",
          "type": "boolean"
        },
        "skill": {
          "default": "",
          "description": "Matching skill from the actor's sheet, or empty for d6.",
          "type": "string"
        },
        "helped": {
          "default": "",
          "description": "Helpful circumstance that adds d6. Empty when using `helper_id` or no help.",
          "type": "string"
        },
        "helper_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of an ally here who adds their skill die, or null for no helper."
        },
        "helper_skill": {
          "default": "",
          "description": "Matching skill from the helper's sheet, or empty for their d6.",
          "type": "string"
        },
        "hindered": {
          "default": "",
          "description": "Hindering circumstance that lowers the actor's die to d4, such as an injury trait in the way or a heavy load; empty when nothing hinders.",
          "type": "string"
        },
        "luck_test": {
          "default": "",
          "description": "Separate bad luck that may arrive, or empty for no test.",
          "type": "string"
        }
      },
      "required": [
        "actor_id",
        "goal",
        "risk",
        "hit"
      ],
      "type": "object"
    }
  },
  {
    "name": "roll_luck_test",
    "description": "Roll a standalone bad-luck test.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "actor_id": {
          "description": "Exact id of the player or actor here facing bad luck.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "subject": {
          "description": "Possible bad luck, such as low ammo or nearby guards.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "actor_id",
        "subject"
      ],
      "type": "object"
    }
  },
  {
    "name": "change_credits",
    "description": "Pay or charge an actor.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "actor_id": {
          "description": "Exact id of the player or an actor here.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "amount": {
          "description": "Positive pays the actor; negative charges them.",
          "type": "integer"
        }
      },
      "required": [
        "actor_id",
        "amount"
      ],
      "type": "object"
    }
  },
  {
    "name": "complete_chapter",
    "description": "Record that the current job has ended.",
    "parameters": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "buy_gear",
    "description": "Buy one catalogue item at its printed price, charged to the buyer. Most cost ₡1. For sale: vest, battle-armor (₡2), hardsuit (₡3), cyber-ear, cyber-eye, cyber-limb, cranial-jack, healing-nanobots, toxin-filter, voice-mimic, flamethrower, low-g-jetpack, med-scanner, mini-drone, repair-tools, survey-pack, grenades, pistol, rifle, shotgun, stun-baton, tranq-gun. Every starship already has the basic version of each function below; one of their upgrades costs ₡10 and needs `onto_id`. Comms: Upgrade with eavesdropper, jammer, tachyon-burst. Crafts: Comes with escape pod. Upgrade with fighter, shuttle. Drive: FTL jump and sublight speeds. Upgrade with longer-jumps, faster-speed, greater-agility. Equipment: Vac suits for crew. Upgrade with armory, heavy-loader, mining-gear, tow-cable. Hull armor: Breaks harmlessly for defense. Upgrade with reentry-rated, sun-shielding. Sensors: Upgrade with deep-space, life-sign-scan, planetary-survey, tactical-vessel-scan. Weapons: Deflector turrets. Upgrade with laser-cutter, military-grade-turret, torpedos.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "actor_id": {
          "description": "Exact id of the player or an actor here who pays.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "gear_id": {
          "description": "Exact id of a catalogue entry.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "onto_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of the ship a ship upgrade is installed in, or null to be carried."
        }
      },
      "required": [
        "actor_id",
        "gear_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "advance",
    "description": "Spend one advance a party member has earned, when the player asks for it. Choose one skill this job improves. Raise an existing skill one step or add a new one at d8.",
    "parameters": {
      "additionalProperties": false,
      "description": "The skill increase earned by one job.",
      "properties": {
        "subject_id": {
          "description": "Exact id of the party member who advances.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "type": "string"
        },
        "skill": {
          "description": "Title-case skill to raise one step or add at d8.",
          "minLength": 1,
          "type": "string"
        },
        "why": {
          "description": "One short reason, in the fiction, for the change.",
          "type": "string"
        }
      },
      "required": [
        "subject_id",
        "skill",
        "why"
      ],
      "type": "object"
    }
  }
]
