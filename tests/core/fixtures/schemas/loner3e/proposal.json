{
  "$defs": {
    "Change": {
      "additionalProperties": false,
      "description": "One sheet change earned by the adventure.",
      "properties": {
        "kind": {
          "description": "Type of sheet change.",
          "enum": [
            "skill",
            "gear",
            "frailty",
            "rewrite"
          ],
          "title": "Kind",
          "type": "string"
        },
        "tag": {
          "description": "New title-case tag, or the exact current tag for a rewrite.",
          "minLength": 1,
          "title": "Tag",
          "type": "string"
        },
        "into": {
          "default": "",
          "description": "New title-case tag for a rewrite. Empty for other kinds.",
          "title": "Into",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "tag"
      ],
      "title": "Change",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "All sheet changes earned by one adventure.",
  "properties": {
    "changes": {
      "description": "One to four earned changes, in reading order.",
      "items": {
        "$ref": "#/$defs/Change"
      },
      "maxItems": 4,
      "minItems": 1,
      "title": "Changes",
      "type": "array"
    },
    "why": {
      "description": "One short reason shown to the player before confirmation.",
      "title": "Why",
      "type": "string"
    }
  },
  "required": [
    "changes",
    "why"
  ],
  "title": "AdventureGrowth",
  "type": "object"
}
