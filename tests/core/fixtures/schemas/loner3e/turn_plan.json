{
  "$defs": {
    "AdvanceThread": {
      "additionalProperties": false,
      "description": "Move a storyline the scenario is tracking: where it stands now, or that it is over.",
      "properties": {
        "op": {
          "const": "advance-thread",
          "default": "advance-thread",
          "title": "Op",
          "type": "string"
        },
        "thread_id": {
          "description": "Exact id of one thread in ACTIVE THREADS.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Thread Id",
          "type": "string"
        },
        "status": {
          "anyOf": [
            {
              "enum": [
                "active",
                "resolved",
                "dormant"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Where the thread now stands, or null to leave it as it is.",
          "title": "Status"
        },
        "stage": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Stable slug for the point it has reached, or null to leave it as it is.",
          "title": "Stage"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "thread_id"
      ],
      "title": "AdvanceThread",
      "type": "object"
    },
    "CounterChange": {
      "additionalProperties": false,
      "description": "Move a counter: `adjust` shifts it by a delta, clamped to the counter's own bounds;\n`spend` pays a cost from it and refuses when the pool cannot cover it.",
      "properties": {
        "op": {
          "const": "counter-change",
          "default": "counter-change",
          "title": "Op",
          "type": "string"
        },
        "mode": {
          "description": "`adjust` moves the pool, `spend` pays from it and can refuse.",
          "enum": [
            "adjust",
            "spend"
          ],
          "title": "Mode",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the entity affected; an actor must be here with the player.",
          "title": "Entity Id",
          "type": "string"
        },
        "counter": {
          "description": "Exact key of one of that entity's counters, as its state spells it.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Counter",
          "type": "string"
        },
        "amount": {
          "description": "For `adjust`, how much the pool moves: negative to reduce. For `spend`, how much of the pool is paid, always positive.",
          "title": "Amount",
          "type": "integer"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "mode",
        "entity_id",
        "counter",
        "amount"
      ],
      "title": "CounterChange",
      "type": "object"
    },
    "GainImprovisedItem": {
      "additionalProperties": false,
      "description": "Give the player an ordinary incidental object that is not in canon and is not worth a canon\nentry of its own. Never a substitute for an item that already exists.",
      "properties": {
        "op": {
          "const": "gain-improvised-item",
          "default": "gain-improvised-item",
          "title": "Op",
          "type": "string"
        },
        "item_name": {
          "description": "The object written out, such as 'a handful of gravel'.",
          "minLength": 1,
          "title": "Item Name",
          "type": "string"
        }
      },
      "required": [
        "item_name"
      ],
      "title": "GainImprovisedItem",
      "type": "object"
    },
    "Loner3eEffect": {
      "discriminator": {
        "mapping": {
          "advance-thread": "#/$defs/WorldOp",
          "counter-change": "#/$defs/CounterChange",
          "gain-improvised-item": "#/$defs/WorldOp",
          "move": "#/$defs/WorldOp",
          "relation-change": "#/$defs/WorldOp",
          "reveal": "#/$defs/WorldOp",
          "trait-change": "#/$defs/WorldOp"
        },
        "propertyName": "op"
      },
      "oneOf": [
        {
          "$ref": "#/$defs/WorldOp"
        },
        {
          "$ref": "#/$defs/CounterChange"
        }
      ]
    },
    "Move": {
      "additionalProperties": false,
      "description": "Move an actor who actually changes location, or one item within the player's reach: picked\nup, set down here, or handed to an actor here. Moving the player to an unrevealed location\nreveals it.",
      "properties": {
        "op": {
          "const": "move",
          "default": "move",
          "title": "Op",
          "type": "string"
        },
        "entity_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of the actor or item that moves; null moves the player. An item must be one the player carries, or one loose at their location.",
          "title": "Entity Id"
        },
        "to_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of where it goes: for an actor the location they enter; for an item an actor here with the player, or the player's own location to set it down. An actor always names one; null hands the item to the player.",
          "title": "To Id"
        }
      },
      "title": "Move",
      "type": "object"
    },
    "OutcomeBranch_Loner3eEffect_": {
      "additionalProperties": false,
      "properties": {
        "outcome": {
          "description": "One outcome label the chosen action allows.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Outcome",
          "type": "string"
        },
        "effects": {
          "default": [],
          "description": "What that outcome causes in the world.",
          "items": {
            "$ref": "#/$defs/Loner3eEffect"
          },
          "title": "Effects",
          "type": "array"
        }
      },
      "required": [
        "outcome"
      ],
      "title": "OutcomeBranch[Loner3eEffect]",
      "type": "object"
    },
    "Question": {
      "additionalProperties": false,
      "description": "A closed dramatic question, answered by Chance d6 against Risk d6.",
      "properties": {
        "act": {
          "const": "question",
          "default": "question",
          "title": "Act",
          "type": "string"
        },
        "actor_id": {
          "description": "Exact id of the actor the question is about: the player, or an actor here.",
          "title": "Actor Id",
          "type": "string"
        },
        "question": {
          "description": "The closed dramatic question the dice answer, phrased so that yes is what the actor wants.",
          "minLength": 1,
          "title": "Question",
          "type": "string"
        },
        "leverage": {
          "default": [],
          "description": "Tags that make this easier, each copied exactly as it is written on the actor's sheet or on a trait in the scene. Empty when none applies; you cannot invent one.",
          "items": {
            "type": "string"
          },
          "maxItems": 3,
          "title": "Leverage",
          "type": "array"
        },
        "trouble": {
          "default": [],
          "description": "Tags that make this harder, copied the same way. Empty when none applies.",
          "items": {
            "type": "string"
          },
          "maxItems": 3,
          "title": "Trouble",
          "type": "array"
        },
        "opponent_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of the actor opposing this, set only when the question is one exchange of a conflict; the engine then takes luck off whichever side loses it.",
          "title": "Opponent Id"
        }
      },
      "required": [
        "actor_id",
        "question"
      ],
      "title": "Question",
      "type": "object"
    },
    "RelationChange": {
      "additionalProperties": false,
      "description": "Record, break, unblock, or reveal a lasting tie between two entities. Containment is not a\ntie: carrying an item or standing somewhere are moves. `reveal` shows the player a way through\nthey did not know about; write it before moving them through a passage they have not found.\n`untag` lifts a block such as `locked` when the fiction opens it.",
      "properties": {
        "op": {
          "const": "relation-change",
          "default": "relation-change",
          "title": "Op",
          "type": "string"
        },
        "mode": {
          "description": "What happens to the tie: `add` records it, `remove` breaks it, `untag` lifts a tag it carries, `reveal` shows it to the player.",
          "enum": [
            "add",
            "remove",
            "untag",
            "reveal"
          ],
          "title": "Mode",
          "type": "string"
        },
        "kind": {
          "description": "What the tie is: `connected` joins two locations the player can walk between, `party-member` puts an actor here into the player's party (source is the actor, target is `player`).",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Kind",
          "type": "string"
        },
        "source": {
          "description": "Exact id of the tie's source entity.",
          "title": "Source",
          "type": "string"
        },
        "target": {
          "description": "Exact id of the tie's target entity.",
          "title": "Target",
          "type": "string"
        },
        "tag": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "For `untag` only: the exact id of a tag the tie carries, such as `locked`.",
          "title": "Tag"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "mode",
        "kind",
        "source",
        "target"
      ],
      "title": "RelationChange",
      "type": "object"
    },
    "Reveal": {
      "additionalProperties": false,
      "description": "Reveal an entity that exists but the player does not know yet: they notice it, are told of\nit, or reach it. Prefer this over inventing a replacement.",
      "properties": {
        "op": {
          "const": "reveal",
          "default": "reveal",
          "title": "Op",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the unrevealed canon entity.",
          "title": "Entity Id",
          "type": "string"
        }
      },
      "required": [
        "entity_id"
      ],
      "title": "Reveal",
      "type": "object"
    },
    "TraitChange": {
      "additionalProperties": false,
      "description": "Put a lasting condition, edge, or burden on an entity, or lift one the fiction ends. The\ntrait shows the id written out: `battle-worn` appears as Battle Worn.",
      "properties": {
        "op": {
          "const": "trait-change",
          "default": "trait-change",
          "title": "Op",
          "type": "string"
        },
        "mode": {
          "description": "`add` puts the trait on, `remove` lifts one the entity carries.",
          "enum": [
            "add",
            "remove"
          ],
          "title": "Mode",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the entity affected; an actor must be here with the player.",
          "title": "Entity Id",
          "type": "string"
        },
        "trait_id": {
          "description": "Stable slug for the trait, such as `poisoned`; to remove, the exact id of a trait the entity carries.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Trait Id",
          "type": "string"
        },
        "text": {
          "default": "",
          "description": "The constraint or benefit it puts on the entity, in prose. Adding only.",
          "title": "Text",
          "type": "string"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "mode",
        "entity_id",
        "trait_id"
      ],
      "title": "TraitChange",
      "type": "object"
    },
    "WorldOp": {
      "anyOf": [
        {
          "$ref": "#/$defs/Reveal"
        },
        {
          "$ref": "#/$defs/Move"
        },
        {
          "$ref": "#/$defs/GainImprovisedItem"
        },
        {
          "$ref": "#/$defs/TraitChange"
        },
        {
          "$ref": "#/$defs/RelationChange"
        },
        {
          "$ref": "#/$defs/AdvanceThread"
        }
      ]
    }
  },
  "additionalProperties": false,
  "properties": {
    "effects": {
      "default": [],
      "description": "Consequences that happen whatever the action settles.",
      "items": {
        "$ref": "#/$defs/Loner3eEffect"
      },
      "title": "Effects",
      "type": "array"
    },
    "branches": {
      "default": [],
      "description": "Consequences keyed by outcome: at most one branch per label, and only labels this action allows. Outcomes that do not occur simply never apply. An action rolled to cause a lasting change — a condition taking hold or lifted, something revealed — must carry that change in the matching outcome's branch: with no branch, even success changes nothing.",
      "items": {
        "$ref": "#/$defs/OutcomeBranch_Loner3eEffect_"
      },
      "title": "Branches",
      "type": "array"
    },
    "action": {
      "anyOf": [
        {
          "$ref": "#/$defs/Question"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The one question this turn resolves, or null when nothing is uncertain enough to ask."
    }
  },
  "title": "TurnPlan",
  "type": "object"
}
