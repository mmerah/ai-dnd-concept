{
  "$defs": {
    "AdvanceThread": {
      "additionalProperties": false,
      "description": "Move a storyline the scenario is tracking: where it stands now, or that it is over.",
      "properties": {
        "op": {
          "const": "advance-thread",
          "default": "advance-thread",
          "title": "Op",
          "type": "string"
        },
        "thread_id": {
          "description": "Exact id of one thread in ACTIVE THREADS.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Thread Id",
          "type": "string"
        },
        "status": {
          "anyOf": [
            {
              "enum": [
                "active",
                "resolved",
                "dormant"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Where the thread now stands, or null to leave it as it is.",
          "title": "Status"
        },
        "stage": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Stable slug for the point it has reached, or null to leave it as it is.",
          "title": "Stage"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "thread_id"
      ],
      "title": "AdvanceThread",
      "type": "object"
    },
    "Attack": {
      "additionalProperties": false,
      "description": "One attack with a carried weapon, or one attack line off the attacker's own stat block.",
      "properties": {
        "act": {
          "const": "attack",
          "default": "attack",
          "title": "Act",
          "type": "string"
        },
        "actor_id": {
          "description": "Exact id of the attacker, here with the player.",
          "title": "Actor Id",
          "type": "string"
        },
        "target_id": {
          "description": "Exact id of what is attacked, here with the player.",
          "title": "Target Id",
          "type": "string"
        },
        "weapon_item_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of a weapon item the attacker carries. Null when the attack comes from a stat block instead.",
          "title": "Weapon Item Id"
        },
        "attack_bonus": {
          "anyOf": [
            {
              "maximum": 30,
              "minimum": -10,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "To-hit bonus copied off a stat block's attack line, such as 4 for `Bite +4 to hit`. Null whenever a weapon item is named.",
          "title": "Attack Bonus"
        },
        "damage": {
          "anyOf": [
            {
              "examples": [
                "1d8",
                "2d6 + 3",
                "1d4 - 1"
              ],
              "maxLength": 64,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Damage dice copied off that same line, such as `1d4+2`. Null whenever a weapon item is named.",
          "title": "Damage"
        },
        "two_handed": {
          "default": false,
          "description": "True only for a versatile weapon wielded in both hands.",
          "title": "Two Handed",
          "type": "boolean"
        },
        "mode": {
          "default": "normal",
          "description": "`advantage` in melee on a target tagged `prone` or `restrained`, or otherwise helpless, and on any target that cannot see the attacker; `disadvantage` when the attacker is tagged `poisoned` or `frightened`, or shoots at a `prone` target; `normal` otherwise. One roll either way.",
          "enum": [
            "normal",
            "advantage",
            "disadvantage"
          ],
          "title": "Mode",
          "type": "string"
        }
      },
      "required": [
        "actor_id",
        "target_id"
      ],
      "title": "Attack",
      "type": "object"
    },
    "CastSpell": {
      "additionalProperties": false,
      "description": "One spell, resolved from its record: the engine spends the slot before anything follows.",
      "properties": {
        "act": {
          "const": "cast-spell",
          "default": "cast-spell",
          "title": "Act",
          "type": "string"
        },
        "actor_id": {
          "description": "Exact id of the caster, here with the player.",
          "title": "Actor Id",
          "type": "string"
        },
        "spell": {
          "description": "The spell's content ref, written `pack/collection/index` exactly as rendered.",
          "minLength": 1,
          "title": "Spell",
          "type": "string"
        },
        "slot_level": {
          "anyOf": [
            {
              "maximum": 9,
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The spell-slot level it is cast from; a higher one upcasts it. Null for a cantrip, which spends nothing.",
          "title": "Slot Level"
        },
        "target_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of the one creature it is aimed at, here with the player. Null only when the spell touches nobody.",
          "title": "Target Id"
        }
      },
      "required": [
        "actor_id",
        "spell"
      ],
      "title": "CastSpell",
      "type": "object"
    },
    "Check": {
      "additionalProperties": false,
      "description": "An ability check or a saving throw against a difficulty the Director sets. The roll\nsettles only what `branches` write: a check made to gain or shed a condition needs that\ntag change in its `success` branch.",
      "properties": {
        "act": {
          "const": "check",
          "default": "check",
          "title": "Act",
          "type": "string"
        },
        "actor_id": {
          "description": "Exact id of the actor rolling, here with the player.",
          "title": "Actor Id",
          "type": "string"
        },
        "bonus": {
          "description": "The actor's own bonus: their ability modifier, plus `proficiency-bonus` only where they are proficient.",
          "maximum": 30,
          "minimum": -10,
          "title": "Bonus",
          "type": "integer"
        },
        "dc": {
          "description": "What the roll must reach: 5 easy, 10 moderate, 15 hard, 20 very hard.",
          "maximum": 40,
          "minimum": 1,
          "title": "Dc",
          "type": "integer"
        },
        "reason": {
          "description": "What is being tested, in a few words.",
          "minLength": 1,
          "title": "Reason",
          "type": "string"
        },
        "mode": {
          "default": "normal",
          "description": "`advantage` when the fiction plainly favours the actor — a helpless subject, surprise, the right tool to hand; `disadvantage` when a tag such as `poisoned` or `frightened` works against them; `normal` otherwise. One roll either way.",
          "enum": [
            "normal",
            "advantage",
            "disadvantage"
          ],
          "title": "Mode",
          "type": "string"
        }
      },
      "required": [
        "actor_id",
        "bonus",
        "dc",
        "reason"
      ],
      "title": "Check",
      "type": "object"
    },
    "CounterChange": {
      "additionalProperties": false,
      "description": "Move a counter: `adjust` shifts it by a delta, clamped to the counter's own bounds;\n`spend` pays a cost from it and refuses when the pool cannot cover it.",
      "properties": {
        "op": {
          "const": "counter-change",
          "default": "counter-change",
          "title": "Op",
          "type": "string"
        },
        "mode": {
          "description": "`adjust` moves the pool, `spend` pays from it and can refuse.",
          "enum": [
            "adjust",
            "spend"
          ],
          "title": "Mode",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the entity affected; an actor must be here with the player.",
          "title": "Entity Id",
          "type": "string"
        },
        "counter": {
          "description": "Exact key of one of that entity's counters, as its sheet spells it.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Counter",
          "type": "string"
        },
        "amount": {
          "description": "For `adjust`, how much the pool moves: negative to reduce. For `spend`, how much of the pool is paid, always positive.",
          "title": "Amount",
          "type": "integer"
        },
        "maximum": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A new upper bound for the pool. Adjust, and advancement only.",
          "title": "Maximum"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "mode",
        "entity_id",
        "counter",
        "amount"
      ],
      "title": "CounterChange",
      "type": "object"
    },
    "GainImprovisedItem": {
      "additionalProperties": false,
      "description": "Give the player an ordinary incidental object that is not in canon and is not worth a canon\nentry of its own. Never a substitute for an item that already exists.",
      "properties": {
        "op": {
          "const": "gain-improvised-item",
          "default": "gain-improvised-item",
          "title": "Op",
          "type": "string"
        },
        "item_name": {
          "description": "The object written out, such as 'a handful of gravel'.",
          "minLength": 1,
          "title": "Item Name",
          "type": "string"
        }
      },
      "required": [
        "item_name"
      ],
      "title": "GainImprovisedItem",
      "type": "object"
    },
    "Improvise": {
      "additionalProperties": false,
      "description": "The roll for anything the other actions do not model, this engine's rules included.",
      "properties": {
        "act": {
          "const": "improvise",
          "default": "improvise",
          "title": "Act",
          "type": "string"
        },
        "dice": {
          "description": "The whole expression, bonuses included: `1d20 + 3`.",
          "examples": [
            "1d8",
            "2d6 + 3",
            "1d4 - 1"
          ],
          "maxLength": 64,
          "title": "Dice",
          "type": "string"
        },
        "vs": {
          "anyOf": [
            {
              "maximum": 40,
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The number the total must reach; null when nothing is contested.",
          "title": "Vs"
        },
        "reason": {
          "description": "What is being rolled, in a few words.",
          "minLength": 1,
          "title": "Reason",
          "type": "string"
        },
        "mode": {
          "default": "normal",
          "description": "`advantage` when the fiction plainly favours the actor — a helpless subject, surprise, the right tool to hand; `disadvantage` when a tag such as `poisoned` or `frightened` works against them; `normal` otherwise. One roll either way.",
          "enum": [
            "normal",
            "advantage",
            "disadvantage"
          ],
          "title": "Mode",
          "type": "string"
        }
      },
      "required": [
        "dice",
        "reason"
      ],
      "title": "Improvise",
      "type": "object"
    },
    "Move": {
      "additionalProperties": false,
      "description": "Move an actor who actually changes location, or one item within the player's reach: picked\nup, set down here, or handed to an actor here. Moving the player to an unrevealed location\nreveals it.",
      "properties": {
        "op": {
          "const": "move",
          "default": "move",
          "title": "Op",
          "type": "string"
        },
        "entity_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of the actor or item that moves; null moves the player. An item must be one the player carries, or one loose at their location.",
          "title": "Entity Id"
        },
        "to_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact id of where it goes: for an actor the location they enter; for an item an actor here with the player, or the player's own location to set it down. An actor always names one; null hands the item to the player.",
          "title": "To Id"
        }
      },
      "title": "Move",
      "type": "object"
    },
    "OutcomeBranch": {
      "additionalProperties": false,
      "description": "What follows in the fiction if the action lands on this outcome, and only then.",
      "properties": {
        "outcome": {
          "description": "One outcome label the chosen action allows.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Outcome",
          "type": "string"
        },
        "effects": {
          "default": [],
          "description": "What that outcome causes in the world.",
          "items": {
            "$ref": "#/$defs/TurnEffect"
          },
          "title": "Effects",
          "type": "array"
        }
      },
      "required": [
        "outcome"
      ],
      "title": "OutcomeBranch",
      "type": "object"
    },
    "RelationChange": {
      "additionalProperties": false,
      "description": "Record, break, unblock, or reveal a lasting tie between two entities. Containment is not a\ntie: carrying an item or standing somewhere are moves. `reveal` shows the player a way through\nthey did not know about; write it before moving them through a passage they have not found.\n`untag` lifts a block such as `locked` when the fiction opens it.",
      "properties": {
        "op": {
          "const": "relation-change",
          "default": "relation-change",
          "title": "Op",
          "type": "string"
        },
        "mode": {
          "description": "What happens to the tie: `add` records it, `remove` breaks it, `untag` lifts a tag it carries, `reveal` shows it to the player.",
          "enum": [
            "add",
            "remove",
            "untag",
            "reveal"
          ],
          "title": "Mode",
          "type": "string"
        },
        "kind": {
          "description": "What the tie is: `connected` joins two locations the player can walk between, `party-member` puts an actor here into the player's party (source is the actor, target is `player`).",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Kind",
          "type": "string"
        },
        "source": {
          "description": "Exact id of the tie's source entity.",
          "title": "Source",
          "type": "string"
        },
        "target": {
          "description": "Exact id of the tie's target entity.",
          "title": "Target",
          "type": "string"
        },
        "tag": {
          "anyOf": [
            {
              "maxLength": 64,
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "For `untag` only: the exact id of a tag the tie carries, such as `locked`.",
          "title": "Tag"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "mode",
        "kind",
        "source",
        "target"
      ],
      "title": "RelationChange",
      "type": "object"
    },
    "Rest": {
      "additionalProperties": false,
      "description": "A rest the fiction has finished, refilling what that rest restores and nothing else.\nA turn that prepares and then sleeps is a rest, not a check on the preparations.",
      "properties": {
        "act": {
          "const": "rest",
          "default": "rest",
          "title": "Act",
          "type": "string"
        },
        "actor_id": {
          "description": "Exact id of the resting actor, here with the player.",
          "title": "Actor Id",
          "type": "string"
        },
        "label": {
          "description": "Which rest was taken.",
          "enum": [
            "short-rest",
            "long-rest"
          ],
          "title": "Label",
          "type": "string"
        }
      },
      "required": [
        "actor_id",
        "label"
      ],
      "title": "Rest",
      "type": "object"
    },
    "Reveal": {
      "additionalProperties": false,
      "description": "Reveal an entity that exists but the player does not know yet: they notice it, are told of\nit, or reach it. Prefer this over inventing a replacement.",
      "properties": {
        "op": {
          "const": "reveal",
          "default": "reveal",
          "title": "Op",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the unrevealed canon entity.",
          "title": "Entity Id",
          "type": "string"
        }
      },
      "required": [
        "entity_id"
      ],
      "title": "Reveal",
      "type": "object"
    },
    "TagChange": {
      "additionalProperties": false,
      "description": "Put a lasting condition, edge, or burden on an entity, or lift one the fiction ends. The\nsheet shows the id written out: `battle-worn` appears as Battle Worn.",
      "properties": {
        "op": {
          "const": "tag-change",
          "default": "tag-change",
          "title": "Op",
          "type": "string"
        },
        "mode": {
          "description": "`add` puts the tag on, `remove` lifts one the entity carries.",
          "enum": [
            "add",
            "remove"
          ],
          "title": "Mode",
          "type": "string"
        },
        "entity_id": {
          "description": "Exact id of the entity affected; an actor must be here with the player.",
          "title": "Entity Id",
          "type": "string"
        },
        "tag_id": {
          "description": "Stable slug for the tag, such as `poisoned`; to remove, the exact id of a tag the entity carries.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Tag Id",
          "type": "string"
        },
        "text": {
          "default": "",
          "description": "The constraint or benefit it puts on the entity, in prose. Adding only.",
          "title": "Text",
          "type": "string"
        },
        "why": {
          "default": "",
          "description": "One short sentence saying what causes this change, for the player.",
          "title": "Why",
          "type": "string"
        }
      },
      "required": [
        "mode",
        "entity_id",
        "tag_id"
      ],
      "title": "TagChange",
      "type": "object"
    },
    "TurnEffect": {
      "discriminator": {
        "mapping": {
          "advance-thread": "#/$defs/AdvanceThread",
          "counter-change": "#/$defs/CounterChange",
          "gain-improvised-item": "#/$defs/GainImprovisedItem",
          "move": "#/$defs/Move",
          "relation-change": "#/$defs/RelationChange",
          "reveal": "#/$defs/Reveal",
          "tag-change": "#/$defs/TagChange"
        },
        "propertyName": "op"
      },
      "oneOf": [
        {
          "$ref": "#/$defs/Reveal"
        },
        {
          "$ref": "#/$defs/Move"
        },
        {
          "$ref": "#/$defs/GainImprovisedItem"
        },
        {
          "$ref": "#/$defs/CounterChange"
        },
        {
          "$ref": "#/$defs/TagChange"
        },
        {
          "$ref": "#/$defs/RelationChange"
        },
        {
          "$ref": "#/$defs/AdvanceThread"
        }
      ]
    },
    "UseFeature": {
      "additionalProperties": false,
      "description": "Spend one use of a limited-use feature and apply what its own text gives.",
      "properties": {
        "act": {
          "const": "use-feature",
          "default": "use-feature",
          "title": "Act",
          "type": "string"
        },
        "actor_id": {
          "description": "Exact id of the actor using it, here with the player",
          "title": "Actor Id",
          "type": "string"
        },
        "counter": {
          "description": "Exact key of that feature's counter, as the sheet spells it.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "title": "Counter",
          "type": "string"
        },
        "heal": {
          "anyOf": [
            {
              "examples": [
                "1d8",
                "2d6 + 3",
                "1d4 - 1"
              ],
              "maxLength": 64,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The healing dice the feature's own text gives, bonuses included, such as `1d10 + 3`. Null when it heals nothing.",
          "title": "Heal"
        }
      },
      "required": [
        "actor_id",
        "counter"
      ],
      "title": "UseFeature",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "effects": {
      "default": [],
      "description": "Consequences that happen whatever the action settles.",
      "items": {
        "$ref": "#/$defs/TurnEffect"
      },
      "title": "Effects",
      "type": "array"
    },
    "branches": {
      "default": [],
      "description": "Consequences keyed by outcome: at most one branch per label, and only labels this action allows. Outcomes that do not occur simply never apply. An action rolled to cause a lasting change — a condition taking hold or lifted, something revealed — must carry that change in the matching outcome's branch: with no branch, even success changes nothing.",
      "items": {
        "$ref": "#/$defs/OutcomeBranch"
      },
      "title": "Branches",
      "type": "array"
    },
    "action": {
      "anyOf": [
        {
          "discriminator": {
            "mapping": {
              "attack": "#/$defs/Attack",
              "cast-spell": "#/$defs/CastSpell",
              "check": "#/$defs/Check",
              "improvise": "#/$defs/Improvise",
              "rest": "#/$defs/Rest",
              "use-feature": "#/$defs/UseFeature"
            },
            "propertyName": "act"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/Attack"
            },
            {
              "$ref": "#/$defs/CastSpell"
            },
            {
              "$ref": "#/$defs/Check"
            },
            {
              "$ref": "#/$defs/UseFeature"
            },
            {
              "$ref": "#/$defs/Rest"
            },
            {
              "$ref": "#/$defs/Improvise"
            }
          ]
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The one action this turn resolves, or null when nothing needs resolving.",
      "title": "Action"
    }
  },
  "title": "TurnPlan",
  "type": "object"
}
