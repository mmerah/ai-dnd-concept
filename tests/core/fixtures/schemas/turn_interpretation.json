{
  "$defs": {
    "MechanicStep": {
      "additionalProperties": false,
      "properties": {
        "tool": {
          "description": "Name of the tool that records this step.",
          "minLength": 1,
          "title": "Tool",
          "type": "string"
        },
        "instruction": {
          "description": "One sentence telling the Director what to make it do, with exact ids.",
          "maxLength": 300,
          "minLength": 1,
          "title": "Instruction",
          "type": "string"
        },
        "when": {
          "default": "",
          "description": "Empty when the step always happens. Otherwise the outcome it waits on, in the words this engine's rules use for how the dice land.",
          "maxLength": 120,
          "title": "When",
          "type": "string"
        }
      },
      "required": [
        "tool",
        "instruction"
      ],
      "title": "MechanicStep",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "mechanics": {
      "default": [],
      "description": "The mechanics this turn calls for, in the order they happen.",
      "items": {
        "$ref": "#/$defs/MechanicStep"
      },
      "title": "Mechanics",
      "type": "array"
    },
    "explanation": {
      "description": "One line saying why that is the right reading.",
      "maxLength": 300,
      "minLength": 1,
      "title": "Explanation",
      "type": "string"
    }
  },
  "required": [
    "explanation"
  ],
  "title": "TurnInterpretation",
  "type": "object"
}
