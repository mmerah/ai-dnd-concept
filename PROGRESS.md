# PROGRESS

Tracks `PLAN.md`. One section per phase; closed phases are recorded in git history, not here.

## Phase 3 — preparation — done, staged

- 2026-08-28: Cairn tree committed whole on `cairn-shelved` (`8a9ed4b`); master clean at `cd24b57`.
- [x] 1. Ported from `cairn-shelved`: `PlayerAction`/`play_action` + sheet buttons + MCP `player_action` (no consumer until Phase 4). Left out as planned: the clock (`Game.hour`, `pass_time`, `advance_clock`, `Engine.elapse`) and everything Cairn. `Entity.uses`/`actions.use_item`/`USE_ITEM` were ported and then deleted again in step 2: their one consumer is 24XX's break budget, which is engine data.
- [x] 2. `ItemSheet`: `SheetEngine[S, I]` and `SheetMechanics[S, I]` gained `item_type` and an `items` dict beside `sheets`, so an item's `rules` validate, seed, refuse and render exactly as an actor's (error shape `items.<id>.<field>`). 24XX declares `ItemSheet` (`bulky`, `broken`, `breaks: Counter`), so the `BULKY`/`BROKEN` traits, `BREAK_TEXT` and the director.md paragraph reserving those slugs are gone; Loner declares `item_type = Sheet` but routes nothing to `items` (`uses_item_sheet` is False): SRD "Everything is a Character" keeps a thing that resists in `sheets`, so core has no `isinstance` special case for it, and `LONER-3E.md` deviation 2 stands. `Entity.uses`, `actions.use_item`, `USE_ITEM` and the prompt's `uses left:` line were deleted with the traits: a break budget is engine data, and `Entity.rules` is now the only engine channel on `Entity`.
- [x] 3. Tool bar in `AGENTS.md` Design rules: one Director tool per SRD procedure, never more than 24XX's eight; a core tool one engine reads is added by that engine.
- [x] 4. `test_package_boundary`'s `ENGINES` derives from `engine_ids()`; each engine's golden-turn `SCRIPT` moved to `tests/<engine>/golden_turn.py`, discovered by `test_golden_turn.py` via `importlib.import_module`; shared script pieces (`NARRATION`, `LISTENING`, `take`) live in new `tests/core/golden_turn_support.py`.
- [x] 5. Docs: `docs/CAIRN-BAREBONES.md`, `docs/FATE-CONDENSED.md`, `plans/L5-*`, `plans/L6-*` deleted; README rows/attribution gone, Breathless rows added; `docs/BREATHLESS.md` added; `IDEAS.md` L5/L6 merged.

- [x] 6. Review fixes, 2026-08-28: `Engine.advancement` is optional (Breathless has none) and `Engine.notes` folds the owed lines in; `actions.discard` removes an item and `Engine.forget` mirrors `seed` so mechanics drop it; `actions.improvise` takes `rules`; `SheetEngine[S, I = ItemBase]` with `item_type = None` by default, so Loner declares nothing about items; `Engine.settle` deleted (no reader); one `offered()` in core behind `GameSession.offers()`, and `play_action` refuses args no offer matches; 24XX `ItemSheet` checks `broken == (breaks.current == 0)`.

## Phase 4 — Breathless — done, staged (eval 96%)

- Note: step 6 above lists `actions.discard`, `Engine.forget` and `improvise(rules)`; none is in the tree at `7d9afeb`. Breathless is built without them: an item reduced to d4 stays in the world as "broken, lost, or faded" (SRD wording), and `seed` refuses a rules-less item created in play, so `gain_improvised_item` is dead for this engine.
- [x] `engines/breathless/`: `rules.py` (Sheet: six skills rated + `worn`, loot die, stress 0/4, stunt, med kit flag; `ItemSheet.die`), `engine.py` (5 Director tools: `stake_check`, `roll_check`, `test_luck`, `loot_check`, `change_stress`; 2 player actions: `catch_breath`, `use_med_kit`; no advancement), `director.md` (3.0 KB), `packs/srd.json` (skills + six rolling tables, transcribed 2026-08-28).
- [x] `characters/kael/breathless.json`; `docs/BREATHLESS.md` deviations (1: Before We Start); README row.
- [x] Scenario `scenarios/saint-ivo/` (single-engine, `grows: true`; 6 locations, 4 actors, 4 items, thread `cold-cache`).
- [x] `tests/breathless/` (golden turn script + 9 engine tests); golden fixtures regenerated; `evals/turn_eval.py` CANON entry, `check_resolved` in ROLL_FACTS, the stake expectation now applies to any engine that declares a `stake_*` tool.
- [x] Verification 2026-08-28: 289 passed, ruff check/format clean, basedpyright clean.
- [x] Eval: 20 Breathless cases in `evals/turn_eval.py` (24 with the shared four): direct roll on an accepted risk, one case per skill choice (Bash/Dash/Sneak/Sway/Think), item die, stunt + spent stunt, helper, luck test, loot (plain / medical / full backpack), stress up and down, vulnerable failure ruling, catch-breath complication, d4 item refused, NPC direct roll, no improvised items. Not directly checkable: the `medical` flag (only lands on a 9+), `dangerous` on its own (scored through the vulnerable warning). Not yet run live.
- [x] Eval v1 (3 repeats, concurrency 12): 62/72 (86%). Misses: stunt not chosen (director.md stunt line sharpened), NPC-threat case was wrong (rewritten: the player's own dangerous check; director.md says a threat is the player's check), catch-breath complication narrated without a mechanic (note now asks for one in the world).
- [x] SRD fidelity audit (Opus): 2 unlisted deviations fixed in code — every roller who is vulnerable is flagged (helper shares the risk; `helper_stunt` added), and a full backpack no longer blocks the loot check (the find lies where the actor stands). Also: a second med kit is refused before the roll, dead `worn` validation deleted, director.md "resets everything" corrected, drop path named. Rulings list in `docs/BREATHLESS.md` rewritten (9 rulings). Deviations stay at 1.
- [x] Case-quality pass (Opus): 21 engine cases + 4 shared; `scavenge-for-a-med-kit` deleted (duplicate), `scavenge-on-a-spent-loot-die` and `use-the-med-kit` added, five prompts rewritten to one defensible reading.
- [x] Eval v2: 69/75 (92%). `use-the-med-kit` 0/3: the Director faked it with `change_stress -3`. Fix: `use_med_kit` is now a Director tool too (6 tools, same resolver as the player action); director.md: an authored hidden thing is `reveal`, not a loot check.
- [x] Maintainer rule: at most 2 deviations, and rulings are not to read as deviations. Two rulings were real deviations and are now code: a 9+ loot roll is a `Loot` decision the player answers (item or med kit — `medical` deleted), and an item worn to d4 leaves the backpack (lies at the location, frees its slot). `docs/BREATHLESS.md`: 1 deviation, 3 one-line rulings (stress capped at 4, luck read on the ladder, med kit as a sheet mark).
- [x] Eval v3: 66/75 (88%), `use-the-med-kit` 3/3. Eval v4: 67/75 (89%) after two wording fixes (throw case is a pure Shoot throw; director.md: a question with nobody acting is a luck test, never a loot check). Run-to-run noise is ±3 cases; the steady miss is the shared `risky-climb` stake judgement (1/3), the same weak-model line 24XX meets.
- [x] Maintainer question: the med kit stays a player action (button + MCP); the Director tool is the same resolver so typed "I use my med kit" cannot be faked with `change_stress`.
- [x] Eval v5 (`breathless-v5`, 3 repeats): 72/75 (96%). Misses: `risky-climb` staked 2/3 (shared stake judgement), `swing-the-fire-axe` 1/3 (the axe was taken but the roll used a skill, not `item_id`). Results in `evals/results/breathless-v{1..5}.json`.
- Not mine, left unstaged: `docs/NEXT-ENGINE-RESEARCH.md` (+55 lines) and untracked `DRASTIC-SIMPLIFICATION-OPINION-1.md`.
