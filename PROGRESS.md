# Phase 1 progress

- [x] Step 1 — extract `with_enum` into `engines/transact.py`, delete `_with_skill_enum`
- [x] Step 2 — narrow `unlock_exit.to_id` via `_narrow_unlock_targets` prepare in `turn/tools.py`
- [x] Step 3 — delete `reveal`-then-`move` clause from `turn/prompts/director.md`
- [x] Verify — regen prompt goldens (schema goldens pin the unprepared vocabulary — unchanged by design), read diffs, gate green (pytest/ruff/format/basedpyright)
- [x] Stage (no commit); live eval n=9 left to maintainer (manual per AGENTS.md)

# Phase 3 progress

- [x] Step — `SceneSnapshot.of` hidden becomes `_reachable_hidden(world, here)`: here, one exit away, or a location signposted from known canon; `canon` stays full
- [x] Verify — director goldens regenerated via `test_golden_turn.py` (the plan's `test_golden_prompts.py` command doesn't cover them); diff is exactly `elena` leaving both engines, `bell_tower`/`vault`/`vault_map`/`cloister_rat` stay; `test_context_boundary.py` unchanged; gate green (184 passed, ruff/format/basedpyright 0/0/0)
- [x] Adversarial review (opus) — correct on all edge cases (rootless non-locations impossible, nesting via `location_of`, player must be known, determinism); docstring trimmed to one line, helper folded to a `near` set + one comprehension; gate re-run green
- [x] Stage (no commit); live eval n=9 (`take-the-chart`, `open-the-way-and-climb`) left to maintainer (manual)

# Phase 2 progress

- [x] Step 1 — required `Scenario.engines` (min_length=1); both shipped `world.json` declare `["loner3e", "twentyfourxx"]`
- [x] Step 2 — `load_catalog` reports `scenario.engines` and skips a scenario naming an uninstalled engine with a warning
- [x] Step 3 — `WorldDraft.engines` (hidden from `as_json`, absent from `ScenarioPatch`), `AuthoringSession.engines`, `playtests(config, engines)`, engine multi-select in `ui/scenario_create.py`
- [x] Step 4 — `CharacterOverlay.entities`, `_require_authored` and `Character._overlay_fits_the_character` deleted; `begin_game` rules are `{PLAYER_ID: overlay.character}`
- [x] Step 5 — `EngineBinding` and `Engine.binding()` deleted; `load_character(directory, name, engine, check_overlay)`; six call sites updated (app/session, evals, four test modules)
- [x] Verify — gate green (pytest/ruff/format/basedpyright 0/0/0); no eval run (turn loop untouched); save goldens unchanged (a save holds `ScenarioMeta`, not `Scenario`)
- [x] Adversarial review — trimmed the fat the phase left: `read_scenarios(directory, engines)` yields the playable subset like `read_characters` does (skip and intersect in one place, duplicate ids collapse), write-only `Character.engine` deleted, `actor_sheets` collapsed to the player, `WorldDraft.engines` dropped for `scenario(engines)`
- [x] Stage (no commit) — src/ nets +1 with the new engine multi-select included, about −30 without it

# Phase 4 progress

- [x] Step 1 — Expander deleted (role, agent, toolset, prompt, `CanonSource`/`OpenSource`/`RecordSource`, `open_source`, `test_expansion.py`); `ExpansionPatch`/`ExitLink` stripped to plain records. `ingest` folded into `whole_text` — `open_source` was its only other caller, so `SourceRecord`/`search`/`render` went with it (−684 lines)
- [x] Step 2 — `Scenario.grows: bool` replaces `ExpansionPolicy`; `drowned-road` declares `"grows": true`; the create form's select becomes a switch
- [x] Step 3 — `frontier(world)` in `state/world.py` (whispering-vault ships at 2, drowned-road at 1)
- [x] Step 4 — `app/prompts/scenario_extend.md` + `extend_brief(before)` (a factory, not a constant: "new" is only meaningful against the world being extended) + `WorldDraft.of_game`, which strips the player and what they carry
- [x] Step 5 — `delta(before, after)` lives in `app/authoring/extend.py`, not `turn/expansion.py`: it takes a `WorldDraft`, and `turn <- app` is the import direction `test_package_boundary.py` enforces
- [x] Step 6 — `compile_extension`, `Extended` trace entry shown in the trace panel, `GameSession._grow`/`_extend`; `_extending` clears only after its own commit, so a compile can never schedule the next one
- [x] Verify — `tests/core/test_extension.py` (4 tests: `of_game` strips the player, `delta` is add-only, one compile lands in the save with `scenarios/` byte-identical, a thick or non-growing world compiles nothing); gate green (179 passed, ruff/format/basedpyright 0/0/0); no golden moved — no prompt or tool vocabulary the goldens pin changed
- [x] Verify by hand — the seam the suite stubs (`of_game` → `world_agent(EXTEND)` → `finish` validator → `delta`) driven once with a `FunctionModel`: patch came back as one new location plus the `cloister → sub_crypt` `ExitLink`. The untouched world refuses with `"the draft plays, but it is under the bar"`, so `begin_game` accepts a live world as a scenario
- [x] Verify — `grep -ri expander` finds nothing; `whispering-vault` frontier 2 / `grows=False`, `drowned-road` frontier 1 / `grows=True` (fires on its first commit, as planned)
- [x] Adversarial review (fable) — `turn/expansion.py` dissolved into `app/authoring/extend.py` (nothing under `turn/` imported it; the whole mechanism is now one 109-line file) and `ExpansionPatch` → `ExtensionPatch`, the last of the Expander's vocabulary; `compile_extension` → `author_extension` (nothing compiles); `GameSession.document` deleted — it derives the path from `config` + `state`; `_tasks` → `_illustrations` and `_extending` → `_extension`, which is the difference the reader kept having to remember. Keeping the two task fields apart is deliberate: nothing ever awaits or queries `_illustrations`, while `_extension` is awaited by `submit`, queried by `_grow`, and cleared only after its own commit
- [x] Adversarial review — two real defects fixed: `_capped` silently truncated (608 chars in, 599 out) under a function promising `whole_text`, so it and `RECORD_CHARS` are gone; and `extend_brief` accepted a way in from an *unfound* location, which leaves `frontier` unchanged and fires a fresh authoring run every commit forever — the bar now mirrors `frontier`'s own "from a known location"
- [x] Adversarial review — one defect the review missed: `of_game` passed the whole party, but `state/actions.py` lets the Director move a companion away from the player, and `Scenario` refuses a party member who is not at the starting location. Extension would then fail silently every turn until the player walked. `of_game` now keeps only those standing with the player; `delta` never reports party, so dropping one costs nothing
- [x] Played `drowned-road` — the design failed in the hand: an authoring run outlasts a turn, so the world changed silently after the turn was over and the trace never showed it. **Reversed the plan's "the player never waits for a compile":** the run now blocks the turn that ran the world thin, announced as a `worldsmith` step beside director/narrator, so the spinner and the badge explain the wait. The whole background apparatus is gone — `_extension`, `_grow`, the `create_task`, the `restart` cancellation, and the `submit` guard. `Extended` now lands in `entries` before `submit` returns, so the trace shows it. Growth triggers on a turn only, never on an advancement commit
- [x] UI defects found in the same session, fixed and verified in a browser: Enter both newlined and sent (`keydown.enter` never prevented the default; a `js_handler` keeps Shift+Enter for newlines, since NiceGUI has no `.exact` modifier); the composer would not clear (`ui.input` is `LOOPBACK = False`, so clearing to a value the server already held short-circuits in `BindableProperty.__set__` and never reaches the client — an explicit `updateValue` push fixes it); scene art ate 66% of the chat column, now a fixed quarter-height corner with the scene text beside it at 26%; and a new or restarted game had no art at all, because `_illustrate` only ever ran from `submit`
- [x] Restart stops paying for art it already has: `FileStore.discard` no longer wipes the save's media directory, and `restart` no longer cancels in-flight illustrations. Both existed to protect an emptied directory that is no longer emptied, and `scene_key` hashes the visible scene precisely so a frame survives being re-entered. Dropping the cancel also removes a latent hole — `restart` is synchronous, so a cancel only lands after it returns, leaving the old key in `Illustrator.generating` long enough for restart's own `illustrate_scene()` to skip as "already generating" and produce no opening art at all
- [x] A turn in flight is now legible: the composer and send button bind `enabled` to `session.busy`, so they grey out and come back on every path `working` takes, the swallowed failure included. `role_badges` becomes `turn_progress`, moved out of the header to sit between the transcript and the composer — the running step is a solid chip with a spinner, a bold label and an mm:ss clock, finished steps collapse to a ✓, and every chip carries a one-sentence hover explaining its role. `worldsmith`'s sentence says outright that minutes are normal and why it is running. The clock matters most for that step: a multi-minute wait with a ticking counter reads as progress, the same wait without one reads as a hang
- [x] Stage (no commit); the `worldsmith` wait is long by design — reconsider if it stops being worth it

# Phase 5 progress

- [x] Step 1 — `complete_chapter` leaves `core_toolset()` for each engine's own `director_toolset`, worded in that engine's vocabulary ("the adventure … has ended" / "the job … is done") via a module-level `apply_complete_chapter`, matching the `apply_restore_luck` / `apply_change_credits` shape already there. `Engine.chapter_ending` and its build-time read are gone; `Advancement.earned` is abstract and each engine's advancement reads its own `Mechanics.of(state).completed.current`, so `advancement.py` no longer imports `sheets`
- [x] Step 1 verify — schema goldens show the tool moving from core's block into each engine's, with the new wording; instruction goldens show the two reworded `complete_chapter` lines. The tool-count line ("this engine gives you two/three") went with them: the tool is now the engine's own, so leaving the count would have made the prompt count wrong. Gate green (181 passed, ruff/format/basedpyright 0/0/0); live eval n=9 left to maintainer
- [x] Steps 2+3 — done in one pass, because splitting them would ship a knowingly-broken tree: `validate`'s body reads `mechanics_type.of(state).sheets`, so the moment `mechanics_type` widens to `type[Mutable]` the sheet policy has to move with it. `Engine` is no longer generic; `sheet_type`, `new_sheet` and `check_mechanics` are gone; `check_overlay`, `opening_mechanics` and `validate` are abstract and each engine writes its own; `seed` is a no-op hook and both engines seed their own sheet. `engines/engine.py` imports nothing from `.sheets`
- [x] Steps 2+3 verify — annotation sweep across 11 modules plus `evals/` and the test support; `app/registry.py` drops both `pyright: ignore[reportAssignmentType]` and the comment above them, since a non-generic `Engine` needs no widening. New `test_only_a_concrete_engine_imports_the_sheet_library` pins the demotion; `tests/core/test_engine.py`'s bare engine is now sheet-less (`NoSheet` deleted) and the declares-nothing engine trips on `mechanics_type`. Gate green (182 passed, ruff/format/basedpyright 0/0/0); goldens re-ran unmoved, as the plan predicted
- [x] Stage (no commit); step 1's live eval n=9 against `baseline.json` left to maintainer — the `complete_chapter` docstring and the two director-prompt lines are the only model-facing change in the phase
- [x] Review pass — the boundary test now resolves `from aidm.engines import sheets` (the exact form core used before the phase; it previously slipped through as `aidm.engines`); both engine director.md drop the hardcoded tool-count line the phase had bumped by hand, and drop their post-outcome restatement of core's "write only what the outcome adds / stop at the player's intent" and the `add_trait` mapping already in core and the tool docstrings — these cuts join the pending eval's model-facing surface
